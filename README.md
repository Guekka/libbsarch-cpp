# An OOP wrapper for libbsarch
libbsarchpp is an object oriented wrapper for libbsarch

## How to use

With CMake: 
Use ``add_subdirectory`` and ``target_link_library``

It is also available on vcpkg
